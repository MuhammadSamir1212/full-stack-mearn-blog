import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  mainContainer: {
    display: "flex",
    alignItems: "center",
  },
  smMargin: {
    margin: "0.5em",
  },
  actionDiv: {
    textAlign: "center",
  },
}));
